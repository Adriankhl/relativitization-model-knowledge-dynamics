rootProject.name = "relativitization-model-knowledge-dynamics"

include(
    "model",
    "simulations",
)

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}
